# 项目配置和开发规范

## 项目技术栈

### 核心框架
- Vue.js 2.6.14
- Vue Router 3.5.1 (Hash 模式，非 History 模式)
- Vue CLI 5.0.9

### 样式处理
- Less 预处理器
- PostCSS、Autoprefixer、CSS Modules 默认支持

### 代码规范
- ESLint Standard 配置
- Lint on save 已启用
- Babel 配置在独立文件中

### 必需依赖
- element-ui (Vue 2.x 版本，使用 `npm install element-ui`)
- echarts (使用 `npm install echarts vue-echarts`)
- axios (用于 RESTful API 请求，使用 `npm install axios`)

## 项目需求

### 1. 登录注册功能
- 实现用户注册和登录页面
- 使用 Element UI 的表单组件
- 前后端交互采用 RESTful API 风格
- 目前使用假数据模拟，后续接入真实后端

### 2. 专利分类预测
- 输入专利摘要
- 返回各类别的概率（使用 ECharts 可视化展示）
- 使用 Element UI 组件构建界面
- RESTful API 风格交互

### 3. 专利检索
- 提供多个输入条件（关键词、分类、日期等）
- 检索结果以列表形式展示
- 使用 Element UI 的表格组件
- RESTful API 风格交互

### 4. 前后端交互
- 统一使用 RESTful API 风格
- 使用 axios 进行 HTTP 请求
- 创建统一的 API 服务层
- 目前使用 mock 数据，后续替换为真实后端接口

## 代码规范

### Vue 组件规范
- 使用单文件组件 (.vue)
- 组件命名使用 PascalCase
- 文件命名使用 PascalCase 或 kebab-case
- 遵循 Vue 2.x 的 Options API 风格
- 使用 Less 编写样式

### 代码风格
- 遵循 JavaScript Standard Style
- 使用 ESLint Standard 配置
- 自动修复 lint 错误

### 文件结构建议
```
src/
├── api/              # API 请求封装
│   └── index.js      # axios 配置和请求方法
├── assets/           # 静态资源
├── components/       # 公共组件
├── router/           # 路由配置
├── store/            # 状态管理（如需要）
├── utils/            # 工具函数
├── views/            # 页面组件
│   ├── Login.vue     # 登录页面
│   ├── Register.vue  # 注册页面
│   ├── PatentClassification.vue  # 专利分类预测
│   └── PatentSearch.vue          # 专利检索
└── main.js           # 入口文件
```

### API 设计规范
- RESTful 风格
- 使用 axios 统一封装
- 请求方法：GET（查询）、POST（创建）、PUT（更新）、DELETE（删除）
- 统一错误处理
- 统一响应数据格式

### 示例 API 结构
```javascript
// api/index.js
import axios from 'axios'

const api = axios.create({
  baseURL: process.env.VUE_APP_API_BASE_URL || '/api',
  timeout: 10000
})

// 用户相关
export const userApi = {
  login: (data) => api.post('/auth/login', data),
  register: (data) => api.post('/auth/register', data)
}

// 专利相关
export const patentApi = {
  classify: (data) => api.post('/patent/classify', data),
  search: (params) => api.get('/patent/search', { params })
}
```

## 开发注意事项

1. **路由配置**
   - 使用 hash 模式（非 history 模式）
   - 路由守卫用于登录验证

2. **Element UI 使用**
   - 在 main.js 中全局引入或按需引入
   - 使用 Element UI 2.x 版本（兼容 Vue 2.x）

3. **ECharts 使用**
   - 用于专利分类概率的可视化展示
   - 可以使用饼图、柱状图等展示各类别概率

4. **Mock 数据**
   - 创建 mock 数据文件或使用 mock 服务
   - 便于后续替换为真实后端接口

5. **响应式设计**
   - 考虑移动端适配
   - 使用 Element UI 的栅格系统

6. **错误处理**
   - 统一的错误提示
   - 网络请求错误处理

## 命名规范

- 组件名：PascalCase（如 `PatentSearch.vue`）
- 变量/函数名：camelCase（如 `getPatentList`）
- 常量：UPPER_SNAKE_CASE（如 `API_BASE_URL`）
- CSS 类名：kebab-case（如 `.patent-list`）

## 提交规范

- 提交信息使用中文或英文
- 清晰描述提交内容

